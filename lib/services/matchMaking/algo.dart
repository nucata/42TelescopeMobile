import 'package:flutter/material.dart';
import 'package:mobile_app/models/models.dart';

matchMakingAlgo(
  List<Skill> skills,
) {
  /* loop trought each skills to check if it's exist */
  for (var skill in skills) {}

  /* if the skill exists add the new user */

  /* if not create a new collection and add the user informations */
}
